<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="statistic">

	<select id="getDownload" resultType="hashMap">
		SELECT TO_CHAR(DOWNLOAD_DATE) DOWNLOAD_DATE, COUNT(*) COUNT
		FROM DOWNLOAD
		GROUP BY TO_CHAR(DOWNLOAD_DATE)
	</select>
	
</mapper>